import { Button } from '../../components/Button/Button';import styles from '../Sections.module.css';export function CTA({content}:{content:{title:string;text:string;button:string}}){return <section className={styles.ctaSection}><div className="container"><div className={`content-column ${styles.cta}`}><h2>{content.title}</h2><p className={styles.body}>{content.text}</p><Button size="large">{content.button}</Button></div></div></section>}
