import { FAQItem } from '../../components/FAQItem/FAQItem';import styles from '../Sections.module.css';export function FAQ({content}:{content:{title:string;items:{question:string;text:string}[]}}){return <section id="faq" className={`${styles.section} ${styles.faq}`}><div className="container"><div className="content-column"><h2 className={styles.heading}>{content.title}</h2><div className={styles.faqList}>{content.items.map(item=><FAQItem key={item.question} question={item.question} answer={item.text}/>)}</div></div></div></section>}
