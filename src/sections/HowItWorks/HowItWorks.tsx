import { StepCard } from '../../components/StepCard/StepCard';import styles from '../Sections.module.css';export function HowItWorks({content}:{content:{title:string;text:string;items:{title:string;text:string}[]}}){return <section id="how-it-works" className={`${styles.section} ${styles.how}`}><div className="container"><div className="content-column"><div className={styles.howHeader}><h2 className={styles.heading}>{content.title}</h2><p className={styles.body}>{content.text}</p></div><div className={styles.stepGrid}>{content.items.map((item,index)=><StepCard key={item.title} number={index+1} {...item}/>)}</div></div></div></section>}
