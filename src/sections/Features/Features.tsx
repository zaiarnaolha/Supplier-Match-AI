import { FeatureCard } from '../../components/FeatureCard/FeatureCard';import styles from '../Sections.module.css';export function Features({content}:{content:{title:string;items:{title:string;text:string}[]}}){return <section id="features" className={`${styles.section} ${styles.features}`}><div className="container"><div className="content-column"><h2 className={styles.heading}>{content.title}</h2><div className={styles.featureList}>{content.items.map(item=><FeatureCard key={item.title} {...item}/>)}</div></div></div></section>}
