import { Button } from '../../components/Button/Button';import styles from '../Sections.module.css';import laptop from '../../../assets/hero-laptop.png';
export function Hero({content}:{content:{title:string;text:string;primary:string;secondary:string}}){return <section id="top" className={styles.hero}><div className={`container ${styles.heroInner}`}><div className={styles.heroCopy}><h1>{content.title}</h1><p className={styles.body}>{content.text}</p><div className={styles.heroActions}><Button size="large">{content.primary}</Button><Button variant="secondary" size="large" onClick={()=>document.querySelector('#how-it-works')?.scrollIntoView()}>{content.secondary}</Button></div></div><img className={styles.heroImage} src={laptop} alt="Supplier Match AI supplier search interface displayed on a laptop"/></div></section>}
