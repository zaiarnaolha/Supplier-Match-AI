import type { Language } from '../../content';import { Button } from '../../components/Button/Button';import { BrandLogo } from '../../components/BrandLogo/BrandLogo';import { LanguageSwitcher } from '../../components/LanguageSwitcher/LanguageSwitcher';import styles from './Header.module.css';
export function Header({language,onLanguageChange,nav}:{language:Language;onLanguageChange:(l:Language)=>void;nav:{features:string;how:string;faq:string;login:string;signup:string}}){return <header className={styles.header}><div className={`container ${styles.inner}`}><a href="#top"><BrandLogo/></a><nav aria-label="Primary"><a href="#features">{nav.features}</a><a href="#how-it-works">{nav.how}</a><a href="#faq">{nav.faq}</a></nav><div className={styles.actions}><LanguageSwitcher language={language} onChange={onLanguageChange}/><Button variant="ghost" size="small">{nav.login}</Button><Button size="small">{nav.signup}</Button></div></div></header>}
